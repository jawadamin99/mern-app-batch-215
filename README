# **🚀 MERN Blog Application – Docker Setup (Without Docker Compose)**

This guide explains how to run the **Frontend (React/Vite)**, **Backend (Node.js)**, and **MongoDB** using **Docker commands only**.

---

## 📁 Project Structure

```
mern-blog-V1/
 ├── client/        # React (Vite) frontend
 ├── server/        # Node.js backend
```

---

## 🐳 Prerequisites

* Docker installed
* Docker running

---

# 🟢 Step 1: Create Docker Network

All containers must be on the same network to communicate.

```bash
docker network create mern-network
```

---

# 🟢 Step 2: Run MongoDB Container

```bash
docker run -d --name mongodb  --network=mern-network -p 27017:27017  mongo
```

📌 This stores MongoDB data in your local folder.

---

# 🟢 Step 3: Build Backend Image

```bash
docker build -t mern-server .
```

---

# 🟢 Step 4: Run Backend Container

```bash
docker run -d  --name mern-server  --network mongo-net  -p 5001:5000 mern-server
```

📌 Backend connects to MongoDB using container name `mongodb`.

---

# 🟢 Step 5: Build Frontend Image

```bash
docker build -t mern-client .
```

---

# 🟢 Step 6: Run Frontend Container

```bash
docker run -d --name frontend --network mern-network  -p 3000:80  mern-client
```

---

# 🌐 Access Application

* Frontend → http://localhost:3000
* Backend → http://localhost:5001
* MongoDB → localhost:27017

---

# 🧪 Useful Commands

## View running containers

```bash
docker ps
```

## View logs

```bash
docker logs mern-client
docker logs mern-server
docker logs mongodb
```

## Access container shell

```bash
docker exec -it mern-server sh
docker exec -it mongodb bash
```

---

# 🛑 Stop Containers

```bash
docker stop mern-client mern-server mongodb
```

---

# ❌ Remove Containers

```bash
docker rm mern-client mern-server mongodb
```

---

# ❌ Remove Images

```bash
docker rmi mern-client mern-server
```

---

# ⚠️ Common Issues

## 1. MongoDB connection error

✔ Ensure containers are on same network
✔ Use:

```
mongodb://mongodb:27017
```

---

## 2. Frontend shows 404

✔ If using Vite → ensure `dist/` is served (not `build/`)

---

## 3. Port already in use

Change ports:

```bash
-p 3000:3000
```

---

# 💡 Notes

* This setup is for **learning and development**
* For production:

  * Use optimized images (nginx for frontend)
  * Avoid bind mounts for databases
  * Use orchestration tools

---

# 🎯 Summary

You manually created:

* Docker network
* MongoDB container
* Backend container
* Frontend container

This helps understand how Docker works internally before using automation tools.

---
